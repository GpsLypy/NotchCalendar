# Notch Calendar 1.6.14

macOS 15+ · Apple 芯片与 Intel · build 58 · 2026-09-18

## 更新

- 专注和会议状态在实体刘海两侧以独立紧凑区域显示，中间留空。
- 工作台支持浅色、深色和可选毛玻璃；调整日历、专注、文件及便笺交互。
- 加入离线诗笺、私密收藏和按需上手入口；完善试用与数据导出说明。

## 安装与升级

下载 `NotchCalendar-1.6.14-macos.dmg`，退出旧版后拖入「应用程序」替换。另提供 ZIP 和 SHA-256 校验文件。已有授权和本地数据不会因正常覆盖安装而清除。

**本版采用 ad-hoc 签名，未进行 Developer ID 签名或 Apple 公证，需手动安装；macOS 可能显示安全提示，不提供自动替换安装。**

## 验证范围

应所有者要求，本版未运行 Swift 或 Python 测试。发布前仅进行本地构建及发行产物必要检查；这不等同于功能验收。

本版静置性能样本、实体刘海与外接屏交互、真实付款发码、钥匙串权限和 VoiceOver **仍待验收**。所有者要求跳过测试和验收直接发布；不复用旧样本，不豁免实测失败。

## English

Focus and meeting indicators now appear as separate compact islands beside the notch. The workspace offers light, dark, and optional glass styling, alongside refinements to Calendar, Focus, Files, and Scratchpad, plus offline poetry and private bookmarks.

This is an ad-hoc signed, manual-install build without Developer ID signing or Apple notarization. At the owner's request, Swift and Python tests were not run for this version. Only the local build and required artifact checks were performed. Stationary performance samples, physical-notch and external-display interaction, live payment and activation, Keychain permissions, and VoiceOver remain **pending** under the owner's explicit version-specific authorization. No measured failures are waived.
